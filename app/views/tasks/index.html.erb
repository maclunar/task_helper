<h1>Tasks</h2>

<%= link_to 'New task', new_task_path %>

<br><br>

<table>
  <tr>
    <th>Number</th>
    <th>Title</th>
  </tr>

  <% @tasks.each do |task| %>
    <tr>
      <td><%= task.number %></td>
      <td><%= task.title %></td>
      <td><%= link_to 'Show', task_path(task) %></td>
      <td><%= link_to 'Edit', edit_task_path(task) %></td>
      <td><%= link_to 'Delete', task_path(task), method: :destroy,
                        data: { confirm: 'Are you sure?' } %></td>
    </tr>
  <% end %>
</table>

